{
  "name": "@rainmaker/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest --config jest.config.js",
    "test:watch": "jest --config jest.config.js --watch",
    "lint": "eslint . --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "24.0.3",
    "bun-types": "^1.0.25",
    "jest": "^29.7.0",
    "prisma": "^5.10.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}