{
  "name": "@f8n/rainmaker-executor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/action-executor-jobs.ts",
    "build": "bun build src/action-executor-jobs.ts --outdir=dist",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@trigger.dev/sdk": "3.3.17",
    "axios": "1.7.2",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@trigger.dev/testing": "3.3.12",
    "@types/bun": "latest",
    "msw": "2.0.0",
    "typescript": "5.7.2"
  }
}
