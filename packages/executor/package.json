{
  "name": "@f8n/rainmaker-executor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@rainmaker/schema": "workspace:*",
    "@rainmaker/workflow-state": "workspace:*"
  },
  "devDependencies": {
    "@trigger.dev/testing": "3.3.12",
    "@types/bun": "latest",
    "msw": "2.0.0",
    "typescript": "5.7.2"
  }
}
